``` mermaid
sequenceDiagram
    actor PlayerActor as 플레이어 (Actor)
    participant UI as 길드가입_UI
    participant Ctrl as 전투
    participant PObj as 플레이어
    participant GObj as 길드

    %% 1. 플레이어가 UI를 통해 가입을 '요청'함
    PlayerActor->>UI: 길드 가입 요청 (플레이어id, 길드명)
    activate UI
    
    %% 2. UI는 컨트롤러에게 가입 가능 여부를 '확인 및 처리'해달라고 요청 (메서드명 명확화)
    UI->>Ctrl: 가입조건검증 요청(플레이어id, 길드명)
    activate Ctrl
    
    %% [필수 조건 1] 플레이어 자체를 체크
    Ctrl->>PObj: 플레이어체크(플레이어id)
    activate PObj
    PObj-->>Ctrl: true (정상 플레이어 확인)
    deactivate PObj
    
    

    %% [필수 조건 3] 길드 내부의 데이터(중복 여부, 인원수) 확인
    Ctrl->>GObj:현재 인원수 확인()
    activate GObj

    %% 3. 모든 필수 조건이 "전부 부합"했을 때만 비로소 가입을 실행!
    alt 조건 충족: [플레이어 정상] AND [현재 인원 < 5]
        
        %% 여기서 진짜 길드 가입 처리가 일어납니다.
        Ctrl->>GObj: 캐릭터가입(character)
        activate GObj
        GObj-->>Ctrl: 가입 완료 (DB 및 리스트 반영)
        deactivate GObj
        
        GObj-->>UI: 가입 성공 결과 반환
        UI-->>PlayerActor: 화면 표시 ("길드 가입이 완료되었습니다!")
        
    else 조건 미부합: 정원 초과(인원 >= 5)인 경우
    
        %% 조건이 안 맞으면 가입 시키지 않고 실패 메시지만 보냄
        GObj-->>UI: 가입 실패 결과 반환 (사유 명시)
        UI-->>PlayerActor: 화면 표시 ("가입 조건을 만족하지 못했습니다.")
        
    end
    
    deactivate Ctrl
    deactivate UI