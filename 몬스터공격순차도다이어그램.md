sequenceDiagram
    autonumber
    actor User as 사용자
    participant UI as 몬스터_공격_UI
    participant B as 전투
    participant P as 플레이어
    participant C as 캐릭터

    User->>UI: 공격버튼클릭()
    activate UI
    
    UI->>B: 몬스터공격(캐릭터)
    activate B
    
    %% 1. 플레이어 체크 필수 수행
    B->>P: 플레이어체크(플레이어id)
    activate P
    P-->>B: true (검증 성공)
    deactivate P

    %% 2. 조건문(alt)을 통한 흐름 제어
    alt 플레이어 체크 성공 (true)
        %% 3. 직업에 따른 스킬 발동 (다형성)
        B->>C: 스킬발동()
        activate C
        Note right of C: 다형성에 의해<br/>전사 또는 마법사 스킬 발동
        C-->>B: 데미지 반환
        deactivate C
        
        %% 4. 데미지에 따른 등급 부여
        B->>B: 등급부여(데미지)
        activate B
        Note right of B: 내부 메서드 호출로<br/>등급(String) 산출
        deactivate B
        
        B-->>UI: 공격 결과 및 등급 반환
    else 플레이어 체크 실패 (false)
        B-->>UI: 오류 메시지 반환 (인증 실패)
    end
    deactivate B
    
    UI->>UI: 화면표시()
    UI-->>User: 전투 결과 화면 노출
    deactivate UI