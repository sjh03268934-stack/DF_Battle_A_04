```mermaid
classDiagram
    direction TB

    %% ==========================================
    %% 경계 클래스 (Boundary / UI)
    %% ==========================================
    class Create_Character_UI {
        <<boundary>>
        +화면표시()
    }
    class Attack_Monster_UI {
        <<boundary>>
        +화면표시()
    }
    class Add_Item_UI {
        <<boundary>>
        +화면표시()
    }
    class Join_Guild_UI {
        <<boundary>>
        +화면표시()
    }

    %% ==========================================
    %% 제어 클래스 (Control)
    %% ==========================================
    class 전투 {
        +캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨) String
        +몬스터공격(플레이어id) String
        +아이템획득(플레이어id, 아이템명, 타입, 가치) String
        +길드가입(플레이어id, 길드명) String
        -등급부여(데미지) String
    }

    %% ==========================================
    %% 엔티티 클래스 (Entity)
    %% ==========================================
    class 플레이어 {
        -플레이어id : String
        +플레이어체크(플레이어id) boolean
    }

    class 캐릭터 {
        <<abstract>>
        -이름 : String
        -레벨 : int
        -hp : int
        -공격력 : int
        -소유인벤토리 : 인벤토리
        +스킬발동()* int
        +스킬명반환()* String
    }

    class 전사 {
        +스킬발동() int
        +스킬명반환() String
    }

    class 마법사 {
        +스킬발동() int
        +스킬명반환() String
    }

    class 인벤토리 {
        -아이템리스트 : List~아이템~
        -최대용량 : int
        +아이템추가(새아이템 : 아이템) boolean
    }

    class 아이템 {
        -아이템명 : String
        -타입 : String
        -가치 : int
        -등급 : String
    }

    class 길드 {
        -길드명 : String
        -캐릭터리스트 : List~캐릭터~
        -최대인원 : int
        +캐릭터가입(가입캐릭터 : 캐릭터) boolean
    }

    %% ==========================================
    %% 관계 설정 (Relationships)
    %% ==========================================
    
    %% UI -> 제어 클래스 (의존 관계)
    Create_Character_UI ..> 전투 : 캐릭터 생성 요청
    Attack_Monster_UI ..> 전투 : 공격 요청
    Add_Item_UI ..> 전투 : 아이템 처리 요청
    Join_Guild_UI ..> 전투 : 가입 처리 요청

    %% 제어 클래스 -> 엔티티 (연관 관계)
    전투 --> 플레이어 : 인증플레이어 보유
    전투 --> 캐릭터 : 현재캐릭터 관리
    전투 --> 길드 : 서버길드 관리

    %% 엔티티 간의 관계
    플레이어 "1" --> "0..*" 캐릭터 : 소유
    캐릭터 <|-- 전사 : 상속
    캐릭터 <|-- 마법사 : 상속
    
    %% 요구사항 반영 (합성 및 집합)
    캐릭터 "1" *-- "1" 인벤토리 : 소유 (Composition)
    인벤토리 "1" *-- "0..*" 아이템 : 포함 (Composition)
    길드 "1" o-- "0..*" 캐릭터 : 소속 (Aggregation)