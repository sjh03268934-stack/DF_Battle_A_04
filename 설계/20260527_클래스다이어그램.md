``` mermaid
classDiagram
    direction TB

    %% ==========================================
    %% 경계 클래스 (Boundary / UI)
    %% ==========================================
    class 캐릭터_생성_UI {
        <<boundary>>
        +화면표시()
        +캐릭터정보입력()
    }
    class 몬스터_공격_UI {
        <<boundary>>
        +화면표시()
        +공격버튼클릭()
    }
    class 아이템추가_UI {
        <<boundary>>
        +화면표시()
    }
    class 길드가입_UI {
        <<boundary>>
        +화면표시()
    }

    %% ==========================================
    %% 제어 클래스 (Control)
    %% ==========================================
    class 전투 {
        +캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
        +몬스터공격(캐릭터)
        +아이템획득()
        +길드가입()
        -등급부여(데미지) String
    }

    %% ==========================================
    %% 엔티티 클래스 (Entity)
    %% ==========================================
    class 플레이어 {
        -플레이어id : String
        +플레이어체크(플레이어id) boolean
    }

    class 캐릭터 {
        <<abstract>>
        -캐릭터명 : String
        -레벨 : int
        -HP : int
        -공격력 : int
        -인벤토리 : 인벤토리
        +스킬발동()*
    }

    class 전사 {
        +스킬발동()
    }

    class 마법사 {
        +스킬발동()
    }

    class 인벤토리 {
        -아이템리스트 : List~아이템~
        -최대용량 : int
        +아이템추가(item : 아이템)
    }

    class 아이템 {
        -아이템명 : String
        -타입 : String
        -가치 : int
        -등급 : String
    }

    class 길드 {
        -길드명 : String
        -캐릭터리스트 : List~캐릭터~
        -최대인원 : int
        +캐릭터가입(character : 캐릭터)
    }

    %% ==========================================
    %% 관계 설정 (Relationships)
    %% ==========================================
    
    %% UI -> 제어 클래스 (의존 관계)
    캐릭터_생성_UI ..> 전투 : 캐릭터 생성 요청
    몬스터_공격_UI ..> 전투 : 공격 요청
    아이템추가_UI ..> 전투 : 아이템 처리 요청
    길드가입_UI ..> 전투 : 가입 처리 요청

    %% 제어 클래스 -> 엔티티 (의존 및 연관 관계)
    전투 ..> 플레이어 : 검증 (플레이어 체크)
    전투 --> 캐릭터 : 관리 및 전투 수행
    전투 ..> 길드 : 길드 데이터 반영 명령

    %% 엔티티 간의 관계
    플레이어 "1" --> "0..*" 캐릭터 : 소유
    캐릭터 <|-- 전사 : 상속
    캐릭터 <|-- 마법사 : 상속
    
    %% 요구사항 반영 (합성 및 집합)
    캐릭터 "1" *-- "1" 인벤토리 : 소유 (합성)
    인벤토리 "1" o-- "0..*" 아이템 : 포함 (집합)
    길드 "1" o-- "0..*" 캐릭터 : 소속 (집합)