```mermaid
sequenceDiagram
    actor Player as 플레이어
    participant System as 게임 시스템
    participant Character as 캐릭터 (전사/마법사)

    Note over Player, Character: [ 캐릭터 생성 과정 ]
    
    Player->>System: 캐릭터 생성(id, 이름, 직업, 레벨)
    activate System
    System->>System: 플레이어 체크(id == "hero")
    
    alt id == "hero" 일 경우
        System->>Character: 객체 생성 및 기본 능력치 설정
        activate Character
        Character-->>System: 능력치 설정 완료 (직업, 레벨 비례)
        deactivate Character
        System-->>Player: 캐릭터 생성 완료 메시지
    else id != "hero" 일 경우
        System-->>Player: 생성 실패 (권한 없음)
    end
    deactivate System

    Note over Player, Character: [ 몬스터 공격 과정 ]
    
    Player->>System: 몬스터 공격(id)
    activate System
    System->>System: 플레이어 체크(id == "hero")
    
    alt id == "hero" 일 경우
        System->>Character: 스킬 발동 요청()
        activate Character
        Character-->>System: 최종 데미지 반환 (직업별 스킬 계수 적용)
        deactivate Character
        System->>System: 데미지 등급 판정 (S, A, B)
        System-->>Player: 공격 결과 출력 (스킬명, 데미지, 등급)
    else id != "hero" 일 경우
        System-->>Player: 공격 실패 (권한 없음)
    end
    deactivate System