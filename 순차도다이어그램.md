```mermaid
sequenceDiagram
    actor User as 사용자
    participant CreateUI as 캐릭터_생성_UI
    participant AttackUI as 몬스터_공격_UI
    participant Battle as 전투
    participant Player as 플레이어
    participant Char as 캐릭터(전사/마법사)

    Note over User, Char: [ 캐릭터 생성 시나리오 ]
    User->>CreateUI: 캐릭터정보입력(id, 캐릭터명, 직업, 레벨)
    activate CreateUI
    CreateUI->>Battle: 캐릭터생성(id, 캐릭터명, 직업, 레벨)
    activate Battle
    
    Battle->>Player: 플레이어체크(id)
    activate Player
    Note right of Player: id == "hero" 인지 검증
    Player-->>Battle: 검증 결과 반환 (true/false)
    deactivate Player
    
    alt 검증 성공 (true)
        Battle->>Char: 객체 생성 (직업 및 레벨 비례 스탯 부여)
        activate Char
        Char-->>Battle: 캐릭터 생성 완료
        deactivate Char
        Battle-->>CreateUI: 생성 성공 응답
    else 검증 실패 (false)
        Battle-->>CreateUI: 생성 실패 (권한 없음) 응답
    end
    
    CreateUI->>CreateUI: 화면표시()
    CreateUI-->>User: 결과 화면 출력
    deactivate Battle
    deactivate CreateUI
    
    
    Note over User, Char: [ 몬스터 공격 시나리오 ]
    User->>AttackUI: 공격버튼클릭()
    activate AttackUI
    AttackUI->>Battle: 몬스터공격(캐릭터)
    activate Battle
    
    Battle->>Player: 플레이어체크(id)
    activate Player
    Player-->>Battle: 검증 결과 반환 (true/false)
    deactivate Player
    
    alt 검증 성공 (true)
        Battle->>Char: 스킬발동()
        activate Char
        Note right of Char: 전사: 공격력*1.5<br/>마법사: 공격력*2.0
        Char-->>Battle: 데미지 수치 반환
        deactivate Char
        
        Battle->>Battle: 등급부여(데미지)
        Note right of Battle: S급(200↑), A급(100↑), B급(100↓)
        
        Battle-->>AttackUI: 공격 결과 및 등급(String) 반환
    else 검증 실패 (false)
        Battle-->>AttackUI: 공격 실패 응답
    end
    
    AttackUI->>AttackUI: 화면표시()
    AttackUI-->>User: 전투 결과 출력